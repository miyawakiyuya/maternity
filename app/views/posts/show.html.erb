<div class="row">
  <div class="col-3">
    <%= form_with(model:[@post, @post_comment], local: true) do |f| %>
    <h4 class="hading">new comment</h4> <br/>
      <div>
       <%= f.text_area :comment, rows:'3',placeholder: "コメントをここに",class: "postform" %>
      </div>
      <div>
       <%= f.submit '投稿', class: "postform-btn" %>
      </div>
    <% end %>
  </div>
  <div class="table-scroll offset-1 col-7">
    <table id="id">
      <h4>投稿内容</h4>
      <thead>
      <tr><th>投稿日：</th><td><%= @post.created_at.strftime('%Y/%m/%d') %></td></tr>
      <tr><th>投稿者：</th><td><%= @post.user.name %></td></tr>
      <tr><th>タイトル：</th><td><%= @post.title %></td></tr>
      <tr><th>内容：</th><td><%= @post.body %></td></tr>
      </thead>
    </table>
  </div>
</div>



<h4 class="mt-5">コメント一覧</h4>
<div>コメント件数：<%= @post.post_comments.count %></div>
<div>
<table id="id">
  <thead class="table table-bordered">
  <tr class="table-danger">
    <th width="13%">コメント日</th>
    <th width="17%">コメント者</th>
    <th width="40%">コメント内容</th>
    <th width="15%">編集</th>
  </tr>
  </thead>
  <tbody class="table table-bordered">
  <% @post.post_comments.each do |post_comment| %>
  <tr>
   <td><%= post_comment.created_at.strftime('%Y/%m/%d') %></td>
   <td><%= post_comment.user.name %></td>
   <td><%= post_comment.comment %></td>
   <td>
     <% if post_comment.user == current_user %>
      <%= link_to "削除", post_post_comment_path(post_comment.post, post_comment), method: :delete %>
     <% end %>
   </td>
  </tr>
  <% end %>
  </tbody>
</table>
</div>

