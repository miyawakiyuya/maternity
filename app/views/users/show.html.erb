<div class="row">
<div class="col-xs-offset-1 col-xs-8">
  <h4 class="head-line  title_h2 my-5"><span>会員詳細変更</span></h4>
  <table width="700" height="350" >
	<tbody class="offset-1">
     	<tr>
				<td width="30%">会員ID</td>
        <td width="70%"><%= @user.id %></td>
			</tr>
			<tr>
				<td>名前</td>
        <td><%= @user.name %></td>
			</tr>
			<tr>
				<td>メールアドレス</td>
        <td><%= @user.email %> </td>
			</tr>
			<tr>
				<td>会員ステータス</td>
        <td>
        	<% if @user.is_valid %>
			     <p class="text-success">有効</p>
			    <% else %>
			     <p class="text-danger">退会</p>
			    <% end %>
        </td>
			</tr>
			<tr>
        <td></td>
        <td><%= link_to "編集する", edit_users_path, class: "btn btn-success" %>
     </tr>
	</tbody>
  </table>
  </div>